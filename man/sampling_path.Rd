% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_path.R
\name{sampling_path}
\alias{sampling_path}
\title{Path sampling for light-only trajectories}
\usage{
sampling_path(
  tag,
  likelihood = "map_light",
  movement = list(method = "gamma", shape = 1.35, scale = 10.3, low_speed_fix = 0.001,
    zero_speed_ratio = 40),
  move_stay = function(t) 0.17 + (0.68 - 0.17) * (exp(-t/0.78)),
  thr_likelihood = 0.99,
  thr_gs = 1000/24,
  chains = 4,
  iter = 2000,
  warmup = floor(iter/5),
  thin = 1,
  kernel_warmup = 0,
  seed = sample.int(.Machine$integer.max, 1),
  workers = chains,
  refresh = 10,
  future_max_size = 4 * 1024^3,
  quiet = FALSE
)
}
\arguments{
\item{tag}{a GeoPressureR \code{tag} object containing likelihood maps and \code{tag_set_map} parameters.}

\item{likelihood}{Field of the \code{tag} list containing the likelihood map (character). Possible
values are the same as in \code{tag2map()}. Default is \code{"map_light"}.}

\item{movement}{list of movement model parameters passed to \code{speed2prob()}. It must include
\code{method} and the corresponding parameters (e.g., \code{shape}/\code{scale} or \code{location}), plus
\code{low_speed_fix} and \code{zero_speed_ratio}.}

\item{move_stay}{function returning the probability of moving as a function of the stay
duration (days).}

\item{thr_likelihood}{threshold of percentile to keep likely locations in each stationary
period. Use 1 to keep all locations (no masking).}

\item{thr_gs}{maximum ground speed (km/h) used for distance masking and movement kernels.}

\item{chains}{number of chains to run.}

\item{iter}{number of Gibbs iterations.}

\item{warmup}{number of iterations to discard at the beginning.}

\item{thin}{thinning interval for saved samples.}

\item{kernel_warmup}{number of initialization sweeps using a full-grid kernel before the
main iterations.}

\item{seed}{optional integer seed to make chains reproducible.}

\item{workers}{number of workers to use (defaults to \code{chains}). Uses forked workers on Unix
and PSOCK clusters on Windows.}

\item{refresh}{progress message update frequency (in iterations).}

\item{future_max_size}{maximum size (bytes) of globals exported to \code{future} workers. Use
\code{NULL} to keep \code{future.globals.maxSize} unchanged for the current session.}

\item{quiet}{logical to hide progress messages.}
}
\value{
A \code{path} data.frame with columns \code{stap_id}, \code{j}, \code{ind}, \code{lat}, \code{lon}, \code{chain}.
}
\description{
Simulate trajectories from light likelihood maps with a simple movement model and a
single-site Gibbs sampler. The conditional distribution uses the light likelihood at the
current stationary period and movement probabilities to the previous and next positions.
}
\seealso{
Other sampling_path: 
\code{\link{sampling_path_diagnostic}()}
}
\concept{sampling_path}

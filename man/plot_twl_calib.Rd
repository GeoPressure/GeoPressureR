% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_twl_calib.R
\name{plot_twl_calib}
\alias{plot_twl_calib}
\alias{plot_twl_calib_path}
\title{Plot twilight calibration diagnostics}
\usage{
plot_twl_calib(x, path = NULL, warning_ci = 0.95, plot_plotly = TRUE, ...)

plot_twl_calib_path(tag, path, warning_ci = 0.9973, plot_plotly = TRUE)
}
\arguments{
\item{x}{a GeoPressureR \code{twl_calib} object or a \code{tag} object with calibration.}

\item{path}{a GeoPressureR \code{path} or \code{pressurepath} data.frame. If provided, plot the path-based
calibration diagnostic instead of the stacked calibration histogram.}

\item{warning_ci}{Width of the symmetric confidence interval used to flag staps with twilights
outside the calibrated range (e.g., \code{0.95} for 2.5\%/97.5\%).}

\item{plot_plotly}{logical to use \code{plotly}.}

\item{...}{unused}

\item{tag}{a GeoPressureR \code{tag} object with twilight calibration (used by
\code{plot_twl_calib_path()}).}
}
\value{
a ggplot or ggplotly object
}
\description{
This function plots the stacked calibration histogram and fitted density. If a \code{path} is
provided, it instead plots the solar zenith angle histogram derived from \code{tag$twilight} and the
path, with the calibration density overlaid.
}
\examples{
withr::with_dir(system.file("extdata", package = "GeoPressureR"), {
  tag <- tag_create("18LX", quiet = TRUE) |>
    tag_label(quiet = TRUE) |>
    tag_set_map(
      extent = c(-16, 23, 0, 50),
      scale = 5,
      known = data.frame(
        stap_id = 1,
        known_lon = 17.05,
        known_lat = 48.9
      )
    ) |>
    twilight_create() |>
    twilight_label_read() |>
    geolight_map_calibrate()
})

# Histogram + kernel density
plot_twl_calib(tag)

# With fitted location duration of 5 days minimum
tag <- geolight_map_calibrate(tag, fitted_location_duration = 5)

# Path-based diagnostic (uses tag$twilight)
path <- tag$stap[, c("stap_id", "start", "end")]
path$lon <- ifelse(is.na(tag$stap$known_lon), 0, tag$stap$known_lon)
path$lat <- ifelse(is.na(tag$stap$known_lat), 0, tag$stap$known_lat)
plot_twl_calib(tag, path = path, plot_plotly = FALSE)

}
\seealso{
Other geolight: 
\code{\link{geolight_fit_location}()},
\code{\link{geolight_map}()},
\code{\link{geolight_solar}()},
\code{\link{twilight_create}()},
\code{\link{twilight_label_read}()},
\code{\link{twilight_label_write}()}
}
\concept{geolight}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.tag.R
\name{plot_tag_twl_calib}
\alias{plot_tag_twl_calib}
\title{Plot twilight calibration of a \code{tag}}
\usage{
plot_tag_twl_calib(
  tag,
  twilight_line = NULL,
  density_threshold = 0.001,
  plot_plotly = TRUE
)
}
\arguments{
\item{tag}{a GeoPressureR \code{tag} object.}

\item{twilight_line}{optional twilight data.frame, typically created with
\code{\link[=path2twilight]{path2twilight()}}, used to show per-stap solar zenith distributions.}

\item{density_threshold}{numeric multiplier of the maximum calibration
density used to define the x-axis limits (default \code{0.001}).}

\item{plot_plotly}{logical to use \code{plotly}.}
}
\value{
a ggplot or ggplotly object.
}
\description{
This function displays the histogram of calibrated solar zenith angles used by
\code{\link[=geolight_map]{geolight_map()}} together with the fitted kernel density.
}
\details{
If \code{twilight_line} is provided (typically created with \code{\link[=path2twilight]{path2twilight()}}),
the function additionally plots, in facets per stationary period, the
distribution of solar zenith angles for each stap, overlaid with the same
calibration density. This helps to diagnose whether the twilight
distribution at a given stap is compatible with the calibration.
}
\examples{
withr::with_dir(system.file("extdata", package = "GeoPressureR"), {
  tag <- tag_create("18LX", quiet = TRUE) |>
    tag_label(quiet = TRUE) |>
    twilight_create() |>
    twilight_label_read() |>
    tag_set_map(
      extent = c(-16, 23, 0, 50),
      scale = 10,
      known = data.frame(
        stap_id = 1,
        known_lon = 17.05,
        known_lat = 48.9
      )
    ) |>
    geolight_map(quiet = TRUE)

  # Plot only the calibration used by geolight_map()
  plot_tag_twl_calib(tag)
})
}
\seealso{
Other plot_tag: 
\code{\link{plot_tag_acceleration}()},
\code{\link{plot_tag_actogram}()},
\code{\link{plot_tag_light}()},
\code{\link{plot_tag_pressure}()},
\code{\link{plot_tag_temperature}()},
\code{\link{plot_tag_twilight}()}
}
\concept{plot_tag}

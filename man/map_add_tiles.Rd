% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_add_tiles.R
\name{map_add_tiles}
\alias{map_add_tiles}
\title{Add GeoPressureR basemap layers}
\usage{
map_add_tiles(
  map,
  provider = NULL,
  provider_options = leaflet::providerTileOptions(),
  position = "topleft"
)
}
\arguments{
\item{map}{a Leaflet map.}

\item{provider}{optional single provider passed to \code{\link[leaflet:addProviderTiles]{leaflet::addProviderTiles()}}. When \code{NULL},
adds all available GeoPressureR basemap layers.}

\item{provider_options}{options passed to \code{\link[leaflet:addProviderTiles]{leaflet::addProviderTiles()}} when \code{provider} is set.}

\item{position}{position of the basemap layer control.}
}
\value{
A Leaflet map.
}
\description{
Adds all available GeoPressureR basemap layers to a Leaflet map. CARTO, Mapbox, and MapTiler
layers are added only when their API key is stored in the system keyring. Esri layers are always
available as fallbacks.
}
\details{
Store an API key once with \code{\link[keyring:key_set_with_value]{keyring::key_set_with_value()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{keyring::key_set_with_value("CARTO_API_KEY", password = "<your CARTO API key>")
keyring::key_set_with_value("MAPBOX_ACCESS_TOKEN", password = "<your Mapbox token>")
keyring::key_set_with_value("MAPTILER_API_KEY", password = "<your MapTiler API key>")
}\if{html}{\out{</div>}}

Mapbox access tokens are visible in the browser when a map is rendered. Use a public, read-only
token rather than a secret token.
}

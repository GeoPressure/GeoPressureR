% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pressurepath_variable.R
\name{pressurepath_variable_available}
\alias{pressurepath_variable_available}
\title{List the ERA5 variables a backend and product can return}
\usage{
pressurepath_variable_available(
  source = c("api", "arco"),
  era5_dataset = c("single-levels", "land", "both")
)
}
\arguments{
\item{source}{Backend: \code{"api"} or \code{"arco"}.}

\item{era5_dataset}{ERA5 product: \code{"single-levels"}, \code{"land"}, or \code{"both"}.}
}
\value{
A character vector of variable names accepted by \code{variable}, including \code{"altitude"}.
}
\description{
The variables available to \code{\link[=pressurepath_create]{pressurepath_create()}} depend on \strong{both} the backend and the ERA5
product, and the sets genuinely differ. GeoPressureAPI serves whatever bands the underlying
Earth Engine collection carries: 292 for ERA5 single levels against 70 for ERA5-Land, with only
44 in common. \code{era5_dataset = "both"} mosaics ERA5-Land on top of ERA5 but returns ERA5's band
namespace, so its set equals \code{"single-levels"} and ERA5-Land-only bands are dropped. The ARCO
backend reads ECMWF's zarr stores directly and currently maps only surface pressure and 2 m
temperature, so it can return \code{"surface_pressure"} and the derived \code{"altitude"}.
}
\details{
Requesting a variable the chosen product does not have used to fail silently: GeoPressureAPI
returned \verb{200 success} with every array empty — not just the missing one — which arrived as a
\code{pressurepath} whose ERA5 columns were all \code{NA}. \code{\link[=pressurepath_create]{pressurepath_create()}} now validates the
request up front with this list.
}
\examples{
length(pressurepath_variable_available("api", "single-levels"))
length(pressurepath_variable_available("api", "land"))
pressurepath_variable_available("arco", "land")

}
\seealso{
Other pressurepath:
\code{\link[=geopressure_timeseries]{geopressure_timeseries()}},
\code{\link[=plot_pressurepath]{plot_pressurepath()}},
\code{\link[=pressurepath_create]{pressurepath_create()}}
}
\concept{pressurepath}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_stap.R
\name{read_stap}
\alias{read_stap}
\title{Read or validate stap data}
\usage{
read_stap(x, required_cols = c("start", "end"))
}
\arguments{
\item{x}{a GeoPressureR \code{tag} object, a \code{stap} data.frame, or a CSV path.
If a \code{tag} is provided, the default file path is
\verb{./data/stap-label/\{tag$param$id\}.csv}.}

\item{required_cols}{character vector of required columns (default: \code{c("start", "end")}).}
}
\value{
A data.frame containing at least the required columns, sorted by \code{start}.
}
\description{
Reads a CSV file defining coarse stationary periods (\code{stap0}) or validates a data.frame,
including date parsing, ordering, and overlap checks.
}
\examples{
# From a data.frame
stap_df <- data.frame(
  start = as.POSIXct(c("2020-01-01", "2020-01-03"), tz = "UTC"),
  end = as.POSIXct(c("2020-01-02", "2020-01-04"), tz = "UTC")
)
read_stap(stap_df)

# From a CSV path
# read_stap("./data/stap-label/18LX.csv")
}
\seealso{
Other tag: 
\code{\link{print.tag}()},
\code{\link{tag_create}()},
\code{\link{tag_set_map}()},
\code{\link{tag_stap_daily}()}
}
\concept{tag}

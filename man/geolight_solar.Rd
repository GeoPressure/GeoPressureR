% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geolight_solar.R
\name{geolight_solar}
\alias{geolight_solar}
\title{Solar zenith angle using NOAA / Meeus equations}
\usage{
geolight_solar(date, lat, lon)
}
\arguments{
\item{date}{POSIXct vector of times.}

\item{lat}{Numeric vector of latitudes (degrees north).}

\item{lon}{Numeric vector of longitudes (degrees east).}
}
\value{
Solar zenith angle(s) in degrees, corrected for atmospheric refraction.
}
\description{
Compute solar zenith angles from time and location using the formulation
implemented in the NOAA Solar Calculator, itself based on the astronomical
algorithms of Meeus.
}
\details{
The computation is split internally into three steps:
\enumerate{
\item \code{geolight_solar_constants()}: Time-only solar quantities (solar time and declination)
\item \code{geolight_solar_zenith()}: Geometric solar zenith angle
\item \code{geolight_solar_refracted()}: Approximate atmospheric refraction correction
}
\subsection{Conventions}{
\itemize{
\item \code{lon} is expressed in \strong{degrees east} (positive eastward).
\item \code{lat} is expressed in \strong{degrees north}.
\item All angles are in \strong{degrees} unless stated otherwise.
\item \code{date} is provided as POSIXct and internally converted to Julian day.
}

When \code{lat} and \code{lon} are vectors, the result is a 3-D array with dimensions:

\code{c(length(lat), length(lon), length(date))}

When \code{lat} and \code{lon} are scalars, the result reduces to a vector of length
\code{length(date)}.
}

\subsection{Solar time and hour angle}{

Internally, the solar time is computed as NOAA's \emph{true solar time} expressed
as an angle (degrees) at Greenwich, without longitude adjustment. Longitude
enters later through the hour angle:

\code{hour_angle = solar_time + lon - 180}

Values are intentionally not wrapped to \verb{[0, 360)} because all downstream
trigonometric functions are periodic.
}

\subsection{Atmospheric refraction}{

Atmospheric refraction is applied using NOAA's standard piecewise
approximation as a function of solar elevation. This correction is
approximate and does not account for local pressure or temperature.
}
}
\examples{
date <- as.POSIXct(
  c("2020-06-21 12:00:00", "2020-12-21 12:00:00"),
  tz = "UTC"
)
z <- geolight_solar(date, lat = 46, lon = 6)
z

}
\references{
NOAA Global Monitoring Laboratory. Solar Calculation Details.
\url{https://gml.noaa.gov/grad/solcalc/calcdetails.html}

Meeus, J. (1998). \emph{Astronomical Algorithms}. Willmann-Bell.
}
\seealso{
Other geolight: 
\code{\link{geolight_fit_location}()},
\code{\link{geolight_map}()},
\code{\link{plot_twl_calib}()},
\code{\link{twilight_create}()},
\code{\link{twilight_label_read}()},
\code{\link{twilight_label_write}()}
}
\concept{geolight}

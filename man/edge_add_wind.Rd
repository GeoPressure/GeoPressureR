% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edge_add_wind.R
\name{edge_add_wind}
\alias{edge_add_wind}
\title{Retrieve ERA5 variable along edge}
\usage{
edge_add_wind(
  graph,
  edge_s,
  edge_t,
  pressure = NULL,
  variable = c("u", "v"),
  rounding_interval = 60,
  interp_spatial_linear = FALSE,
  return_averaged_variable = lifecycle::deprecated(),
  file = function(stap_id, tag_id) {
    
    glue::glue("./data/wind/{tag_id}/{tag_id}_{stap_id}.nc")
 },
  quiet = FALSE,
  path_model = c("gs", "as_2step")
)
}
\arguments{
\item{graph}{either a \code{tag} or a \code{graph} GeoPressureR object.}

\item{edge_s}{a index of the source node of the edge. Either a vector with 3D index or a matrix
of 3 columns, one for each dimension.}

\item{edge_t}{a index of the target node of the edge. Either a vector with 3D index or a matrix
of 3 columns, one for each dimension.}

\item{pressure}{pressure measurement of the associated \code{tag} data used to estimate the pressure
level (i.e., altitude) of the bird during the flights. This data.frame needs to contain \code{date} as
POSIXt and \code{value} in hPa. If not provided, uses \code{graph$pressure}, assuming that argument \code{graph}
is a GeoPressureR \code{tag} object.}

\item{variable}{list of the variables to extract from \href{https://confluence.ecmwf.int/display/CKB/ERA5:+data+documentation#ERA5:datadocumentation-Table9}{the ERA5 pressure level }
using the \code{shortName} notation: \code{"u"}, \code{"v"},  \code{"t"}, \code{"cc"}, \code{"r"},
\code{"w"}, \code{"ciwc"}, \code{"clwc"}, \code{"q"}, \code{"cswc"}, \code{"d"}, \code{"z"}, \code{"o3"}, \code{"pv"}, \code{"vo"}.}

\item{rounding_interval}{temporal resolution on which to query the variable (min). Default is to
match ERA5 native resolution (1hr).}

\item{interp_spatial_linear}{logical to interpolate the variable linearly over space, if \code{FALSE}
takes the nearest neighbour. ERA5 native resolution is 0.25°}

\item{return_averaged_variable}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} No longer used.}

\item{path_model}{model used to interpolate the bird path during flight. \code{"gs"} assumes constant
groundspeed. \code{"as_2step"} first samples wind on the \code{"gs"} path, then approximately redistributes
along-track progress using a constant airspeed assumption.}

\item{file}{absolute or relative path of the ERA5 wind data file to be downloaded. Function
taking as arguments (1) the stationary period identifier and (2) the tag_id.}

\item{quiet}{logical to hide messages about the progress}
}
\value{
A data.frame with one row per time step, edge, and variable:
\itemize{
\item \code{edge_id} edge index
\item \code{val} value of the variable at each time step
\item \code{pressure} pressure at each time step
\item \code{date} datetime of each time step
\item \code{w} weight for averaging
\item \code{var} variable name
\item \code{lat} latitude at each time step
\item \code{lon} longitude at each time step
}
}
\description{
Reads the NetCDF files and extracts the variable requested along each flight defined by the
edges.
\itemize{
\item Time: linear interpolation using the resolution requested with \code{rounding_interval}
\item Space: nearest neighbour interpolation by default, or bi-linear with \code{pracma::interp2} if
\code{interp_spatial_linear=TRUE}. Note: spatial interpolation is limited to 0.1° for computational
reasons.
\item Pressure/altitude: linear interpolation using the exact \code{pressure} values
}
}
\examples{
\dontrun{
withr::with_dir(system.file("extdata", package = "GeoPressureR"), {
  tag <- tag_create("18LX", quiet = TRUE) |>
    tag_label(quiet = TRUE) |>
    tag_set_map(extent = c(-16, 23, 0, 50), scale = 1) |>
    geopressure_map(quiet = TRUE)
})
graph <- graph_create(tag, quiet = TRUE)
wind <- edge_add_wind(graph, edge_s = graph$s, edge_t = graph$t, quiet = TRUE)
}
}
\seealso{
\href{https://geopressure.org/GeoPressureManual/trajectory-with-wind.html}{GeoPressureManual}
}
\concept{edge}

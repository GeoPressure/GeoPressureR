% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pressurepath_create_arco.R
\name{pressurepath_create}
\alias{pressurepath_create}
\title{Create a pressure path directly from ERA5 ARCO}
\usage{
pressurepath_create(
  tag,
  path = tag2path(tag),
  compute_altitude = TRUE,
  solar_dep = 0,
  era5_dataset = c("both", "land", "single-levels"),
  preprocess = FALSE,
  quiet = FALSE,
  debug = FALSE
)
}
\arguments{
\item{compute_altitude}{Logical scalar. If \code{TRUE}, compute altitude from tag pressure, ERA5
surface pressure, 2 m temperature, and surface geopotential.}

\item{era5_dataset}{ERA5 product to use: \code{"land"} for ERA5-Land, \code{"single-levels"} for global
ERA5, or \code{"both"} to use ERA5-Land over land and global ERA5 elsewhere.}

\item{quiet}{Logical scalar to suppress progress messages.}

\item{debug}{Logical scalar to display the selected Zarr paths and array indices.}
}
\value{
A \code{pressurepath} data.frame with the same standard columns and attributes as
\code{\link[=pressurepath_create]{pressurepath_create()}}. It contains \code{surface_pressure}, \code{surface_pressure_norm}, and, when
\code{compute_altitude = TRUE}, \code{altitude}.
}
\description{
\code{pressurepath_create()} reads the required data directly from the ECMWF Analysis-Ready
Cloud-Optimised (ARCO) archive for its
standard surface-pressure and altitude workflow. It interpolates the supplied path during
flights, groups observations by ERA5 grid cell, and reads the required Zarr chunks directly
from the ECMWF Analysis-Ready Cloud-Optimised (ARCO) archive.
}
\details{
Surface pressure is retrieved for every retained tag observation. Altitude is computed from
tag pressure, ERA5 surface pressure, 2 m temperature, and the time-invariant surface
geopotential using the same barometric equation as \code{\link[=geopressure_timeseries]{geopressure_timeseries()}}. Set
\code{compute_altitude = FALSE} when altitude is not needed.

The returned \code{lat} and \code{lon} remain the supplied or interpolated bird coordinates. ERA5 values
are sampled at the nearest 0.1 degree ERA5-Land cell or 0.25 degree ERA5 cell. With
\code{era5_dataset = "both"}, the ERA5-Land land-sea mask selects ERA5-Land over land and global ERA5
elsewhere.

The function currently supports the standard \code{surface_pressure} and optional \code{altitude}
variables only. Use \code{\link[=pressurepath_create]{pressurepath_create()}} to retrieve its wider set of ERA5 variables.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
withr::with_dir(system.file("extdata", package = "GeoPressureR"), {
  tag <- tag_create("18LX", quiet = TRUE) |> tag_label(quiet = TRUE)
})
path <- data.frame(
  stap_id = tag$stap$stap_id,
  lat = c(48.5, 32.5, 30.5, 49.5, 41.6),
  lon = c(17.5, 13.5, 16.5, 21.5, 12.7)
)
pressurepath <- pressurepath_create(tag, path, quiet = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
Other pressurepath:
\code{\link[=geopressure_timeseries]{geopressure_timeseries()}},
\code{\link[=geopressure_timeseries_api]{geopressure_timeseries_api()}},
\code{\link[=plot_pressurepath]{plot_pressurepath()}},
\code{\link[=pressurepath_create_api]{pressurepath_create_api()}}
}
\concept{pressurepath}
